import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mi_estacionamiento',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
